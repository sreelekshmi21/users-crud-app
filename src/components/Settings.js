import * as React from "react";
import { Card, CardContent } from '@mui/material';
import { Title, useResourceContext } from 'react-admin';


const Settings = () =>{
    const resource = useResourceContext()

    document.getElementById('root').innerHTML = 'SREELEKSHMI'
    return (
        <>
         <h2>SSSSSSSS</h2>
        </>
    )
}

export default Settings;